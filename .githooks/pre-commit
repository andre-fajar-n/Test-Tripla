#!/bin/sh

echo "Running RuboCop..."
bundle exec rubocop

if [ $? -ne 0 ]; then
  echo "❌ RuboCop found issues. Please fix them before committing."
  echo "Run 'bundle exec rubocop -a' or 'make lint-fix' to auto-fix some issues."
  exit 1
fi

echo "✅ RuboCop passed!"
exit 0